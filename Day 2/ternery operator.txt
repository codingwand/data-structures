#include <stdio.h>

int main() {

  char operator = '+';
  int number1 = 10;
  int number2 = 20;
  int result = (operator == '+') ? (num1 + num2) : (num1 - num2);
  printf("%d", result);

  return 0;
}
